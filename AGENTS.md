# AGENTS.md

## 프로젝트 개요
3주 안에 완성하는 라이브 커머스 플랫폼 캡스톤 프로젝트다.

## 서비스 경계
- EKS 서비스는 catalog-service, order-service, live-control-service만 만든다.
- catalog-service는 상품과 라이브 노출 상품 매핑을 담당한다.
- order-service는 주문과 체크아웃 흐름을 담당한다.
- live-control-service는 라이브 세션 메타데이터와 IVS 연계를 담당한다.
- core-api는 만들지 않는다.
- services/ 아래에 stream-service를 만들지 않는다.
- services/ 아래에 ws-service를 만들지 않는다.
- 미디어 전송은 EKS 밖에서 처리한다.
- WebSocket, chat, reaction, presence는 serverless/ 아래에서만 구현한다.

## 작업 방식
- 여러 파일을 건드리는 작업은 먼저 plan을 만든다.
- 한 작업은 가능하면 한 서비스만 수정한다.
- 변경은 작고 확실하게 한다.
- 비즈니스 로직을 바꾸면 테스트를 추가하거나 수정한다.
- 불필요한 대규모 리팩터링을 하지 않는다.
- API 계약을 함부로 깨지 않는다.
- 앱 코드 변경과 k8s 변경은 배포 작업이 아닌 이상 분리한다.
- 환경변수 이름은 명확하게 작성한다.

## 멀티에이전트 규칙
- 서브에이전트는 한 서비스 범위 안에서만 작업한다.
- 서비스 간 계약(API, 이벤트)은 사용자 승인 없이 변경하지 않는다.

## 완료 기준
- 관련 테스트가 통과한다.
- 엔드포인트나 환경변수가 바뀌면 문서를 함께 수정한다.
- 관련 없는 파일은 건드리지 않는다.
