apiVersion: apps/v1
kind: Deployment
metadata:
  name: live-control-service
  namespace: olivelive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: live-control-service
  template:
    metadata:
      labels:
        app: live-control-service
    spec:
      containers:
        - name: live-control-service
          image: 890742607521.dkr.ecr.ap-northeast-2.amazonaws.com/live-control-service:prod
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: app-db
                  key: LIVE_CONTROL_DB_URL
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: app-db
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-db
                  key: DB_PASSWORD
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 40
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: live-control-service
  namespace: olivelive
  labels:
    app: live-control-service
spec:
  selector:
    app: live-control-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080